subproof ContinueTest(2**23) {
    for (int i = 0; i < 5; ++i) {
        for (int j = 0; j < 5; ++j) {
            println(`i:${i} of [0..4] j:${j} of [0..4]`);
            if (i > 1 && i == j) {
                println("i > 1 && i == j   i:", i, "j:", j, " ===> CONTINUE");
                continue;
                println("this line must not be shown");
            }
            println("end-of-loop i, only if no-continue");
        }
    }
}
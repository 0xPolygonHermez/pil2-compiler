subproof FixedInsideContainer(2**6) {

    callme();
    fillme();

}

function callme() {
    int opid = 10;
    container air.myfirstcon.`id${opid}` {
        col fixed L2;
        col fixed M2[3];
    }
}

function fillme() {
    int opid = 10;
    use air.myfirstcon.`id${opid}`;

    col fixed `colid${opid}` = [0,1]...;
    for (int i = 0; i < N; ++i) {
        L2[i] = i;
        M2[0][i] = i;
        M2[1][i] = i+1;
        M2[2][i] = i+2;
    }

    int _a = L2[10];
    int a = M2[1][3];
    println(_a, a, L2[10]);
    println(air.myfirstcon.`id${opid}`.L2[10]);
}

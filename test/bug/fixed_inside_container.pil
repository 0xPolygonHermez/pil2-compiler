subproof FixedInsideContainer(2**6) {

    callme();
    fillme();

}

function callme() {
    container air.myfirtcon {
        col fixed L2;
        col fixed M2[3];
    }
}

function fillme() {
    use air.myfirtcon;


    for (int i = 0; i < N; ++i) {
        L2[i] = i;
        M2[0][i] = i;
        M2[1][i] = i+1;
        M2[2][i] = i+2;
    }

    int _a = L2[10];
    int a = M2[1][3];
    println(_a, a);
}

airtemplate TestA(int N = 2**18) {
    airval stage(1) test;
    airgroupval aggregate(sum) default(4) pepa;
    airgroupval aggregate(sum) default(3) pepe[2];
    // println("TestA");    
    col witness k;
    k === 2;
}

airtemplate TestB(int N = 2**18) {
    // airgroupval aggregate(sum) default(3) pepe;
    println("TestB");    
    col witness k;
    k === 2;
}

airgroup Test {
    TestA();
    TestB();
}